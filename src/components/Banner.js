import "./styling.css";

const Banner = () => {
  return (
    <div>
      <header>Orbit Report </header>
      <p>Click the buttons to check the orbit type!</p>
    </div>
  );
};

export default Banner;
